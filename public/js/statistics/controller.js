statistics.controller('StatisticsCtrl', ['$scope', 'Model', function ($scope, Model) {
    $scope.posts = [
        {
            name:'1'
        },
        {
            name:'asdf'
        }
    ];

    $scope.valid = {
      error: 'sb'
    };
}]);
